# Groq free-tier API key for bill summaries (https://console.groq.com/keys)
# If unset, the build still succeeds and summaries are null (backfilled later).
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Diet session to build (defaults to latest available in the SMRI data)
SESSION=221
